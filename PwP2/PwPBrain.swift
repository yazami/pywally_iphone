//
//  PwPBrain.swift
//  PwP2
//
//  Created by Yahya Azami on 2017-11-20.
//  Copyright © 2017 yazami. All rights reserved.
//

import Foundation
import AVFoundation

struct PwPBrain
{
    
}

/*

import AVFoundation
import CoreMedia
import CoreVideo
import QuartzCore
import UIKit

// This Controller will handle the data retrieval from the
// camera and microphone
class RTCMediaController: NSObject {
    // The WKWebView will be our delegate since we want to
    // pass it the media data once it is ready
    
    var mediaDelegate: Any?
    
    // The AVCaptureSession has to come from the top-most view
    // for some reason, in this case the ViewController,
    // otherwise the captureOutput method is never called.
    func start(with session: AVCaptureSession, forDelegate delegate: Any) {
    }
    
    // Our ViewController will be a delegate of type:
    // AVCaptureVideoDataOutputSampleBufferDelegate
    // and we will call this captureOutput method from
    // the captureOutput called by the ViewController
    func captureOutput(_ output: AVCaptureOutput, didOutput sampleBuffer: CMSampleBuffer, from connection: AVCaptureConnection) {
    }
    
    // The WKWebView will call this method to retrieve the frame
    // data, as well as the frame's width and height
    func getLastFrame() -> [AnyHashable: Any]
    {
    }
}
*/
